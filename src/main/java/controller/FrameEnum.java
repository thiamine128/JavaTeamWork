package controller;

/**
 * The frame type enum.
 */
public enum FrameEnum {
    /**
     * Main frame with a map.
     */
    MainFrame,
    /**
     * Post frame with a posts list.
     */
    PostFrame,
    /**
     * Post view frame with a post.
     */
    PostViewFrame
}
