package controller;

/**
 * The enum Frame enum.
 */
public enum FrameEnum {
    /**
     * Main frame frame enum.
     */
    MainFrame,
    /**
     * Post frame frame enum.
     */
    PostFrame,
    /**
     * Post view frame frame enum.
     */
    PostViewFrame
}
